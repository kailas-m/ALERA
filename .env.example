# Example .env for mailing and server
# If you have an SMTP server, set EMAIL_HOST, EMAIL_PORT, EMAIL_USER, EMAIL_PASS, and optionally EMAIL_SECURE
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-smtp-username@example.com
EMAIL_PASS=your-smtp-password

# EMAIL_FROM will be used as the From address; otherwise EMAIL_USER is used
EMAIL_FROM=webmaster@example.com
# EMAIL_TO is the recipient address for contact form messages (your support inbox)
EMAIL_TO=support@example.com

# Alternatively, for Gmail fallback you can set EMAIL_USER and EMAIL_PASS (app password)
# EMAIL_USER=you@gmail.com
# EMAIL_PASS=your_app_password

PORT=5000
